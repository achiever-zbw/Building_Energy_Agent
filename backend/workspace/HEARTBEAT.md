# Heartbeat Tasks（占位）

在 nanobot 中，此文件由定时心跳读取以执行周期性任务。

**当前后端 Agent（FastAPI + `AgentLoop`）未实现心跳调度。**  
若将来接入类似机制，可在此处列出周期性任务说明；在此之前，本文件 **不参与** `ContextBuilder` 的系统提示词组装（避免模型误以为存在自动定时任务）。

## Active Tasks

（暂无）

## Completed

（暂无）
